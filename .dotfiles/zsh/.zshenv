#added locations to path variable
export PATH=$HOME/.local/bin:$PATH

#dotfiles on path
export ZDOTDIR=$HOME/.dotfiles/zsh

#set defaults
export EDITOR=vim

#man page syntax highlights
export MANPAGER="less -R --use-color -Dd+r -Du+b"
